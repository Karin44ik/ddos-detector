from __future__ import annotations

import streamlit as st


def main() -> None:
    st.set_page_config(page_title="About", layout="wide")
    st.title("ℹ️ Про систему виявлення DoS/DDoS-атак")

    st.markdown(
        """
## Загальна інформація

Даний програмний продукт є результатом виконання магістерської роботи,
присвяченої розробці системи виявлення атак типу **DoS/DDoS** на основі
методів машинного навчання.

Система призначена для аналізу мережевого трафіку, представленого у вигляді
наборів ознак (CSV-файлів), та автоматичної класифікації з’єднань на:
- **нормальні (Normal)**
- **атакуючі (DoS/DDoS)**

---

## Архітектура рішення

Робота системи базується на наступному конвеєрі:

1. **Підготовка даних**
   - використання датасетів CICIDS2017;
   - очищення, нормалізація та формування навчальної і тестової вибірок.

2. **Навчання моделей**
   - Random Forest
   - Support Vector Machine (SVM)
   - XGBoost

3. **Оцінювання якості**
   - метрики: Accuracy, Precision, Recall, F1-score, ROC-AUC, PR-AUC;
   - порівняння моделей за точністю та часом навчання.

4. **Детекція**
   - застосування навченої моделі до нових CSV-файлів;
   - формування файлів з прогнозами (`*.pred.csv`).

5. **Візуалізація результатів**
   - графіки важливості ознак;
   - порівняльні діаграми моделей;
   - інтерактивний інтерфейс Streamlit.

---

## Технології та інструменти

- **Python 3.10+**
- **Scikit-learn**
- **XGBoost**
- **Pandas, NumPy**
- **Matplotlib**
- **Streamlit**

---

## Структура проєкту

- `data/processed/` — підготовлені набори даних
- `models/` — збережені ML-моделі (`.pkl`)
- `reports/` — звіти та графіки експериментів
- `src/` — скрипти підготовки даних, навчання, оцінювання
- `app/` — веб-інтерфейс Streamlit

---

## Призначення інтерфейсу

Веб-інтерфейс Streamlit забезпечує:
- зручний запуск детекції DoS/DDoS-атак;
- перегляд метрик та результатів експериментів;
- візуалізацію даних без необхідності роботи з консоллю.

---

**Автор:** студентка 2 курсу, групи 601-ТН
спеціальності 122   Комп’ютерні науки
Шпіка Каріна

**Рік:** 2025-2026
"""
    )


if __name__ == "__main__":
    main()
